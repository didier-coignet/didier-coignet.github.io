box: wercker/default
build:
  steps:
    - arjen/hugo-build:
        version: "0.14"
        theme: redlounge
deploy:
  steps:
    - lukevivier/gh-pages@0.2.1:
        token: $GIT_TOKEN
        domain: didier-coignet.github.io
        basedir: public